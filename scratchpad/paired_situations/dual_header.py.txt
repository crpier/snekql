from typing import Any, ClassVar, assert_type
from datetime import UTC, datetime
from decimal import Decimal
from snekql import sqlite as native
from scratchpad.paired_situations import dual as d, body as b
from scratchpad.class_body_usage import current as original
from scratchpad.class_body_usage.helpers import insert_generic as original_insert


async def caller(dt: d.sqlite.Transaction, bt: b.sqlite.Transaction, dm: d.mariadb.Transaction, bm: b.mariadb.Transaction, dr: d.UserRow, br: b.User[native.Fetched], do: d.OrderRow, bo: b.Order[native.Fetched]) -> None:
    pass
