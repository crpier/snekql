

async def invalid(transaction: database.Transaction) -> None:
    amount = database.scalar(database.select(Invoice.number).limit(1))
    query = database.select(Account.number, amount, Other.text)
    await transaction.fetch_all(query)  # expect-error
