

async def bypass(transaction: database.Transaction) -> None:
    statement = database.raw("SELECT 1 AS value", validate=Total)
    await transaction.fetch_all(statement, validate=False)  # expect-error
